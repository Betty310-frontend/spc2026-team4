upstream fastapi {
    server fastapi-server:8000;
}

server {
    listen 80;
    server_name my-api-server.cloud;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name my-api-server.cloud;

    ssl_certificate     /etc/letsencrypt/live/my-api-server.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/my-api-server.cloud/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;

    # SSE 스트리밍: 에이전트 타임아웃(90s)보다 크게 설정
    proxy_read_timeout    120s;
    proxy_connect_timeout 10s;
    proxy_send_timeout    120s;

    # HTTP/1.1 keep-alive (SSE 필수)
    proxy_http_version 1.1;
    proxy_set_header Connection "";

    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    location / {
        proxy_pass http://fastapi;
    }

    location /api/v1/chat {
        proxy_pass                http://fastapi;
        proxy_buffering           off;
        proxy_cache               off;
        chunked_transfer_encoding on;
    }
}
